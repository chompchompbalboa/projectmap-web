//-----------------------------------------------------------------------------
// Imports
//-----------------------------------------------------------------------------
import { render } from '../test/library'
import MapNodeDates from './MapNodeDates'

//-----------------------------------------------------------------------------
// Tests
//-----------------------------------------------------------------------------
test('renders using default settings without error', async () => {
  return true
  // render(<MapNodeDates nodeId="nodeId" />)
})

test('displays a start date, end date, and duration', async () => {
  return true
})

// When the start date is changed, correctly updates the start date, duration, and end date
// When the end date is changed, correctly updates the start date, duration, and end date
// When the start date is changed, correctly updates the start date, duration, and end date
